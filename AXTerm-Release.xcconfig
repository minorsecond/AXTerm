//
//  AXTerm-Release.xcconfig
//  AXTerm
//
//  Build settings for Release configuration.
//

// MARK: - Sentry Configuration

// Note: Use $(/) to escape the // in URLs so they are not interpreted as comments.
// Using the same DSN for now; swap for production DSN later if needed.
SENTRY_DSN = https:$(/)$(/)5fb37d1ce28b0cf2345b87e31630a442@o4510791070908416.ingest.us.sentry.io$(/)4510791072940032

// Environment tag sent to Sentry to distinguish events by build type.
SENTRY_ENVIRONMENT = production

// Disable Sentry debug logging in Release builds to reduce noise.
SENTRY_DEBUG = NO

// Sample rate for performance traces (10% in Release to reduce overhead).
// Adjust based on traffic volume and budget.
SENTRY_TRACES_SAMPLE_RATE = 0.1

// Sample rate for continuous profiling (0% in Release to minimize overhead).
// Profiling is resource-intensive; enable sparingly in production.
SENTRY_PROFILES_SAMPLE_RATE = 0.0
